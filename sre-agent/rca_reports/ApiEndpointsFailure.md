# RCA Report: ApiItemsEndpointFailure

**Instance:** i-0c3f590b4a9bbddbd
**Severity:** critical
**Generated:** 2026-04-24 11:31:25 UTC

---

## Timeline of Events
2026-04-24T11:12:05 — Backend started on port 5001. 2026-04-24T11:22:33 — Frontend logged '502 Bad Gateway' due to connection refused on port 5000.

---

## Root Cause
The frontend Nginx proxy is configured to forward requests to the backend on port 5000, causing a connection refusal because the backend is listening on port 5001.

## Contributing Factors
Frontend Nginx configuration explicitly points to backend port 5000, while the backend container (mern-app-testing-backend-1) is running on port 5001 as defined by the PORT environment variable.

---

## Impact
/api/items endpoint returns 502 Bad Gateway. Frontend cannot communicate with backend API.

---

## Metrics Evidence
Frontend log: 'connect() failed (111: Connection refused) while connecting to upstream... http://172.18.0.3:5000/api/items'. Backend env: 'PORT=5001'.

---

## Service Topology
N/A

---

## Remediation

### Immediate Fix
docker stop mern-app-testing-frontend-1 && docker run -d --name mern-app-testing-frontend-1 -e BACKEND_PORT=5001 <image_name> (Note: The actual fix requires updating the Nginx config inside the image or via a volume mount to point to port 5001). For a quick test, confirm if the frontend config can be hot-reloaded or the container recreated with updated config.

### Long-term Fix
Update Nginx configuration to use environment variable for backend port or ensure service discovery/compose configuration aligns the internal ports (frontend's proxy_pass and backend's PORT).

---
*Generated by SRE Agent*
